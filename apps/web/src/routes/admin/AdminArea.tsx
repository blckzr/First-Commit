/**
 * Placeholder for the admin area, kept in its own lazily-loaded chunk so a
 * learner's browser never downloads admin screens (design.md §4.3).
 * The admin shell and its ten screens are tracked in docs/task-tracker.md.
 */
export default function AdminArea() {
  return (
    <main style={{ padding: 32 }}>
      <h1>Admin</h1>
      <p>Not built yet.</p>
    </main>
  );
}
