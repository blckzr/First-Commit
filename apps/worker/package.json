{
  "name": "@first-commit/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local AI worker for First Commit: connects Supabase jobs to Ollama (Qwen3.5)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "tsx src/check-setup.ts",
    "try:feedback": "tsx src/examples/code-feedback.ts",
    "try:roadmap": "tsx src/examples/roadmap.ts",
    "worker": "tsx src/worker.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "judge0:check": "tsx src/judge0/check.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.23.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@first-commit/test-db": "*",
    "@types/node": "^22.20.3",
    "@types/pg": "^8.23.1",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
